<h1>Dogs that need adoption</h1>

<% @dogs.each do |dog| %>
	<%= link_to dog %>
		<p><% @dog.photo1 %> | <%= @dog.name %> | <%= @dog.breed %> | <%= @dog.city %> | <%= @dog.state %> | <%= @dog.country %> </p>
	<% end %>
<% end %>